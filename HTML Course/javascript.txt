dev tools
js engine(jit compilation)
console log

css
	writing-mode: vertical-lr;
	
when using flex, if you want to add gap betweeen containers
	gap: 24px;
	gap-vertical and gap-horizontal is also there
image flipping
	Horizontal and vertical
		transform: scale(-1)
			transform: scaleX(-1)
			transform: scaleY(-1)
			
html {
	scroll-behaviour: smooth;
	}
	
Linear gradient for text
	h1 {
		font-size: 10rem;
		background: linear-gradient(to right, rgb(67,125,205),rgb(68,128,210);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
	
pointer-event: none;
opacity: 0;